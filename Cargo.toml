[package]
name = "stream-analyzer-lite"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["common-controls-v6"] }
tauri-plugin-dialog = "2.6.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }
